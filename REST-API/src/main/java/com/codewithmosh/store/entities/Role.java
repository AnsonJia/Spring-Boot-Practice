package com.codewithmosh.store.entities;

// Represents the valid user roles in the application.
public enum Role {//Using an enum restricts the role to the predefined values only
    USER,
    ADMIN
}
