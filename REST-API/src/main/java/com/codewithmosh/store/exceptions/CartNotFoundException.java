package com.codewithmosh.store.exceptions;

public class CartNotFoundException extends RuntimeException { //custom exception type that inherits runtime exception
    //we don't need extra data or methods right now because its only job is a label to identify this specific error
}
