server:
  port: 8090
spring:
  application:
    name: store
  datasource:
    url: jdbc:postgresql://localhost:5432/store_api #?createDatabaseIfNotExist=true
    username: postgres
    password: Yp040721
  jpa:
    show-sql: true
  jwt: #take the secret from environment variable in .env file so it cant be easily discovered in the source code
    secret: ${JWT_SECRET}